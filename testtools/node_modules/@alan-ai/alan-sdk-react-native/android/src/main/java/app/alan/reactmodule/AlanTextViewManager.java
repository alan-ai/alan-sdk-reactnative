package app.alan.reactmodule;

import android.graphics.Color;
import android.view.View;
import android.widget.FrameLayout;
import android.widget.Toast;

import com.facebook.react.bridge.ReactApplicationContext;
import com.facebook.react.uimanager.SimpleViewManager;
import com.facebook.react.uimanager.ThemedReactContext;


public class AlanTextViewManager extends SimpleViewManager<View> {

  public static final String REACT_CLASS = "RNTAlanText";
  ReactApplicationContext mCallerContext;

  public AlanTextViewManager(ReactApplicationContext reactContext) {
    mCallerContext = reactContext;
  }

  @Override
  public String getName() {
    return REACT_CLASS;
  }

  @Override
  public View createViewInstance(ThemedReactContext context) {
    View v = new View(context);

    FrameLayout.LayoutParams params = new FrameLayout.LayoutParams(0, 0);
//    v.setBackgroundColor(Color.RED);
    v.setLayoutParams(params);

//    v.setOnClickListener(new View.OnClickListener() {
//                           @Override
//                           public void onClick(View v) {
//                             Toast.makeText(context.getApplicationContext(), "HEY", Toast.LENGTH_SHORT).show();
//                           }
//                         }
//    );

    v.setVisibility(View.GONE);

    return v;
  }
}